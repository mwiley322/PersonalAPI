console.log('app.js is connected');

$(document).ready(function() {
console.log('DOM is ready');
  // base API route
  var baseUrl = '/api/pizzas';

  // array to hold pizza data from API
  var allPizzas = [];

}); //closes ready function
